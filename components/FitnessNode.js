import {StyleSheet, Text, SafeAreaView} from 'react-native';
import React from 'react';

const FitnessNode = () => {
  return (
    <SafeAreaView>
      <Text>FitnessNode</Text>
    </SafeAreaView>
  );
};

export default FitnessNode;

const styles = StyleSheet.create({});
